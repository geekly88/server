import { Component } from '@angular/core';
import { RouterTransition } from './../../@providers';

@Component({
    selector : 'registersHistory',
    templateUrl : './registersHistory.html',
    animations : [RouterTransition()]
})
export class RegistersHistoryComponent{
    
}